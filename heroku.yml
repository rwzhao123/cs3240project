setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    S3_BUCKET: my-example-bucket
build:
  docker:
    web: Dockerfile
    
  config:
    RAILS_ENV: development
    FOO: bar
release:
  command:
    - ./python manage.py migrate
  image: Dockerfile
run:
  web: gunicorn mysite.wsgi --log-level debu
  asset-syncer:
    command:
      - python asset-syncer.py
  image: Dockerfile
